services:
  postgresdb:
    image: postgres:15-alpine
    container_name: postgres-logitrack
    restart: always
    ports:
      - "5432:5432"
    environment:
      POSTGRES_DB: logitrack
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres